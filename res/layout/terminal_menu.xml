<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:orientation="horizontal" >

    <ImageButton android:id="@+id/toggle_keyboard" 
        android:layout_width="@dimen/act_bar_height"
        android:layout_height="@dimen/act_bar_height"
        android:background="@drawable/transparent_btn"
        android:src="@drawable/hardware_keyboard"
        android:contentDescription="@string/toggle_keyboard" />

    <ImageButton android:id="@+id/clear_btn" 
        android:layout_width="@dimen/act_bar_height"
        android:layout_height="@dimen/act_bar_height"
        android:background="@drawable/transparent_btn"
        android:src="@drawable/content_discard"
        android:contentDescription="@string/clear" />
</LinearLayout>